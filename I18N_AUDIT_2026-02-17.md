# i18n Audit — 2026-02-17

## Scope
User-facing language consistency audit (KA/EN/RU) across current app.

## Summary
- Translation calls found: **342** keys used via `t('...')`
- Keys defined in `LanguageContext.tsx`: **602**
- **Missing keys:** **36** (used in UI, not defined in translation map)

## Missing translation keys (must add first)
```
added
all
applyFilters
back
bookTour
dailyRent
dangerZone
deleteAccount
describe
download
emailNotifications
floorplans
furniture
garden
gym
irreversibleActions
keyFacts
krtsanisi
land
last12Months
location
manageNotifications
nadzaladevi
parking
pool
priceHistory
pushNotifications
receiveEmailUpdates
receivePushUpdates
rent
rooms
sale
scheduleTour
selectPreferredTime
sqm
tour3d
```

## High-risk hardcoded UI text hotspots
These can break language switching consistency even when `t()` exists elsewhere:

1. `src/app/(marketing)/contact/page.tsx`
   - hardcoded alerts in English (`Please fill in...`, `valid email`, success alert)

2. `src/components/ContactSection.tsx`
   - same hardcoded alert pattern

3. `src/components/Newsletter.tsx`
   - mixed translated + hardcoded English validation alerts

4. `src/app/(marketing)/properties/components/UploadPropertyModal.tsx`
   - hardcoded success/error alerts

5. `src/app/(marketing)/compare/page.tsx`
   - many `t(...) || 'English fallback'` literals (partial localization behavior)

6. `src/app/(marketing)/properties/[id]/page.tsx`
   - many `t(...) ?? 'English fallback'` and labels missing from translations

7. Date/time locale forcing to EN in multiple places:
   - `src/components/event-manager.tsx` uses `toLocaleDateString('en-US')` / `toLocaleTimeString('en-US')`

## Map / listing-related notes (from current code pass)
- `PropertiesGoogleMap.tsx`, `SinglePropertyGoogleMap.tsx`, `PropertyDetailsMap.tsx` have mixed dynamic labels and fallback strings; needs final pass after translation keys are complete.
- `Property submit` related UIs (`UploadPropertyModal.tsx`, `PropertySubmitModal.tsx`) are not fully language-clean yet.

## Recommended execution order
1. Add all missing 36 keys to `LanguageContext.tsx` (KA/EN/RU).
2. Remove hardcoded alert strings and route through translations.
3. Replace `t(...) || '...'` and `t(...) ?? '...'` fallback literals in user-facing pages where key now exists.
4. Normalize locale formatting by selected language (avoid hardcoded `en-US` where UI text is localized).
5. Smoke test matrix (KA/EN/RU) for:
   - Header/Nav
   - Properties list + map + property details
   - Property submit flow
   - Dashboard core cards and tabs

## Update — Fix Batch #1 completed
- Added all previously missing translation keys to `LanguageContext.tsx`.
- Re-ran i18n key audit script:
  - Used keys: **347**
  - Defined keys: **643**
  - Missing keys: **0**
- Replaced hardcoded alert strings with translated keys in:
  - `src/components/ContactSection.tsx`
  - `src/app/(marketing)/contact/page.tsx`
  - `src/components/Newsletter.tsx`
  - `src/app/(marketing)/properties/components/UploadPropertyModal.tsx`

## Current status
Batch #1 done. Next recommended batch: remove UI fallback literals (`t(...) || '...'`) and normalize locale formatting paths.
